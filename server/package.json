{
	"name": "server",
	"version": "1.0.0",
	"description": "backend for the test project",
	"license": "ISC",
	"author": "",
	"type": "module",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node --env-file=./.env server.js",
		"dev": "node --watch-path=./ --env-file=./.env server.js"
	},
	"dependencies": {
		"express": "^5.2.1",
		"mongoose": "^9.3.3"
	}
}